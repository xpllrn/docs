---
title: "Variables"
description: "Global variables available in adore embed and message templates."
icon: "signature"
---

These variables are accessible throughout adore and can be used in any command context which contains a `message` or `embed code` parameter (e.g. `welcome`, `leave`, `boost`, and vanity claim notifications).

<Tabs>
  <Tab title="Guild">
    | Variable                          | Output                                                               |
    | --------------------------------- | -------------------------------------------------------------------- |
    | `{guild.name}`                    | The guild's name                                                     |
    | `{guild.id}`                      | The guild's ID                                                       |
    | `{guild.count}`                   | The guild's member count                                             |
    | `{guild.membercount}`             | Alias for `{guild.count}`                                            |
    | `{guild.shard}`                   | The guild's shard ID on bot                                          |
    | `{guild.owner_id}`                | The guild's owner ID                                                 |
    | `{guild.created_at}`              | The guild's creation date in UTC                                     |
    | `{guild.created_at_timestamp}`    | The guild's creation date in UNIX                                    |
    | `{guild.emoji_count}`             | The guild's emoji count                                              |
    | `{guild.role_count}`              | The guild's role count                                               |
    | `{guild.boost_count}`             | The guild's boost count                                              |
    | `{guild.boost_tier}`              | The guild's boost tier, defaults to N/A if none                      |
    | `{guild.preferred_locale}`        | The guild's preferred locale                                         |
    | `{guild.key_features}`            | The guild's list of features (possibly `N/A`)                        |
    | `{guild.icon}`                    | The guild's icon URL as a PNG (possibly `N/A`)                       |
    | `{guild.banner}`                  | The guild's banner URL as a PNG (possibly `N/A`)                     |
    | `{guild.splash}`                  | The guild's splash URL as a PNG (possibly `N/A`)                     |
    | `{guild.discovery}`               | The guild's discovery splash URL as a PNG (possibly `N/A`)           |
    | `{guild.max_presences}`           | The guild's max presences amount                                     |
    | `{guild.max_members}`             | The guild's max members amount                                       |
    | `{guild.max_video_channel_users}` | The guild's max video channel users                                  |
    | `{guild.afk_timeout}`             | The guild's AFK timeout in seconds                                   |
    | `{guild.afk_channel}`             | The guild's AFK channel (possibly `N/A`)                             |
    | `{guild.channels}`                | The guild's list of text, voice & category channels (possibly `N/A`) |
    | `{guild.channels_count}`          | The guild's total channel count                                      |
    | `{guild.text_channels}`           | The guild's list of text channels                                    |
    | `{guild.text_channels_count}`     | The guild's text channel count                                       |
    | `{guild.voice_channels}`          | The guild's list of voice channels                                   |
    | `{guild.voice_channels_count}`    | The guild's voice channel count                                      |
    | `{guild.category_channels}`       | The guild's list of category channels                                |
    | `{guild.category_channels_count}` | The guild's category channel count                                   |
    | `{guild.vanity}`                  | The guild's vanity URL (custom vanity on the vanity bot)             |
  </Tab>
  <Tab title="User">
    <Warning>
      These are **Discord** user fields. For Last.fm account data in `,lastfm mode`, use `{lastfm.name}`, `{lastfm.avatar}`, `{lastfm.plays}`, etc. See the [Last.fm](/integrations/lastfm#custom-now-playing-embed-personal-premium) page.
    </Warning>

    | Variable                       | Output                                                 |
    | ------------------------------ | ------------------------------------------------------ |
    | `{user}`                       | The user's name (legacy `name#tag` when a discriminator exists)      |
    | `{user.id}`                    | The user's ID                                          |
    | `{user.mention}`               | The user's mention                                     |
    | `{user.name}`                  | The user's name                                        |
    | `{user.tag}`                   | The user's discriminator (`0` for most accounts)                   |
    | `{user.avatar}`                | The user's profile picture                             |
    | `{user.guild_avatar}`          | The user's guild profile picture                       |
    | `{user.display_avatar}`        | The user's available profile picture                   |
    | `{user.joined_at}`             | The user's guild join date in UTC                      |
    | `{user.joined_at_timestamp}`   | The user's guild join date in UNIX                     |
    | `{user.created_at}`            | The user's account creation date in UTC                |
    | `{user.created_at_timestamp}`  | The user's account creation date in UNIX               |
    | `{user.display_name}`          | The user's current display name                        |
    | `{user.nick}`                  | The user's server display name                         |
    | `{user.nickname}`              | Alias for `{user.nick}`                                |
    | `{user.boost}`                 | `Yes`/`No` if the user is boosting the guild           |
    | `{user.boost_since}`           | The user's initial guild boost date                    |
    | `{user.boost_since_timestamp}` | The user's initial guild boost date in UNIX            |
    | `{user.color}`                 | The user's top role hex code                           |
    | `{user.top_role}`              | The user's top role name, defaults to N/A              |
    | `{user.role_list}`             | The user's role list (possibly `N/A`)                  |
    | `{user.role_text_list}`        | The user's role list in text (possibly `N/A`)          |
    | `{user.bot}`                   | `Yes`/`No` if the user is a bot                        |
    | `{user.badges_icons}`          | The user's profile badges with emotes (possibly `N/A`) |
    | `{user.badges}`                | The user's profile badges in text (possibly `N/A`)     |
    | `{user.join_position}`         | The user's join position                               |
    | `{user.join_position_suffix}`  | The user's join position with suffix (e.g. `42nd`)     |
  </Tab>
  <Tab title="Channel">
    | Variable                   | Output                                                    |
    | -------------------------- | --------------------------------------------------------- |
    | `{channel.name}`           | The channel's name                                        |
    | `{channel.id}`             | The channel's ID                                          |
    | `{channel.mention}`        | The channel's mention                                     |
    | `{channel.topic}`          | The channel's topic                                       |
    | `{channel.type}`           | The channel's type (`text`/`news`/`..`)                   |
    | `{channel.category_id}`    | The channel's category ID (possibly `N/A`)                |
    | `{channel.category_name}`  | The channel's category name (possibly `N/A`)              |
    | `{channel.position}`       | The channel's position in the guild channel list          |
    | `{channel.slowmode_delay}` | The channel's slowmode delay in seconds (defaults to `0`) |
  </Tab>
  <Tab title="Date & Time">
    | Variable                  | Output                        |
    | ------------------------- | ----------------------------- |
    | `{date.now}`              | Current date in PST           |
    | `{date.utc_timestamp}`    | Current date as UNIX          |
    | `{date.now_proper}`       | Better formatted date in PST  |
    | `{date.now_short}`        | Short formatted date in PST   |
    | `{date.now_shorter}`      | Shorter formatted date in PST |
    | `{time.now}`              | 12 hour timestamp in PST      |
    | `{time.now_military}`     | 24 hour timestamp in PST      |
    | `{date.utc_now}`          | Current date in UTC           |
    | `{date.utc_now_proper}`   | Better formatted date in UTC  |
    | `{date.utc_now_short}`    | Short formatted date in UTC   |
    | `{date.utc_now_shorter}`  | Shorter formatted date in UTC |
    | `{time.utc_now}`          | 12 hour timestamp in UTC      |
    | `{time.utc_now_military}` | 24 hour timestamp in UTC      |
  </Tab>
  <Tab title="Last.fm">
    <Info>
      Only available in `,lastfm mode` and custom now-playing templates. Do **not** use `{user.plays}`, `{user.url}`, or `{author}` - those are not valid.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{lastfm.name}` | Last.fm username |
    | `{lastfm.avatar}` | Last.fm profile picture |
    | `{lastfm.plays}` | Total scrobbles |
    | `{proper(lastfm.plays)}` | Total scrobbles (formatted) |
    | `{lastfm.artist_crown}` | Crown emoji if you lead the artist in this server |
    | `{lastfm.url}` | Last.fm profile URL |
    | `{track.name}` | Current track title |
    | `{lower(track.name)}` | Track title in lowercase |
    | `{track.url}` | Last.fm track URL |
    | `{track.plays}` | Your plays for the track |
    | `{proper(track.plays)}` | Track plays (formatted) |
    | `{track.spotify_url}` | Spotify track URL |
    | `{track.release_date}` | Release date (Spotify) |
    | `{track.duration}` | Duration (Spotify) |
    | `{artist.name}` | Artist name |
    | `{lower(artist.name)}` | Artist name in lowercase |
    | `{artist.plays}` | Your plays for the artist |
    | `{proper(artist.plays)}` | Artist plays (formatted) |
    | `{artist.url}` | Last.fm artist URL |
    | `{artist.image}` | Artist image (Spotify) |
    | `{album.name}` | Album title |
    | `{lower(album.name)}` | Album title in lowercase |
    | `{album.url}` | Last.fm album URL |
    | `{album.cover}` | Album cover |
    | `{user.display_name}` | Discord display name |
    | `{user.name}` | Discord username |
    | `{user.mention}` | Discord @mention |
    | `{user.avatar}` | Discord avatar URL |
  </Tab>
  <Tab title="Leveling">
    <Info>
      Only available for `,levels message` level-up templates.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{level}` | The member's new level |
    | `{xp}` | The member's total XP |
    | `{level.new_rank}` | Member's server rank at time of level up |
    | `{level.user_xp}` | Member's current XP |
  </Tab>
  <Tab title="Boost">
    <Info>
      Only available for `,boost` messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{boost.count}` | Amount of times the user has boosted this server |
    | `{guild.boost_count}` | Overall guild boost count |
  </Tab>
  <Tab title="Bump Reminder">
    <Info>
      Only available for `,bumpreminder` thank-you and reminder messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{bumpreminder.last_user_bump}` | The last user to bump the server |
    | `{bumpreminder.next_bump}` | A relative timestamp for the next bump |
  </Tab>
  <Tab title="Punishments">
    <Info>
      Only available for `,invoke` channel responses and DMs (e.g. `,invoke ban dm`, `,invoke kick message`).
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{moderator}` | Moderator name who performed the action |
    | `{moderator.name}` | Moderator username |
    | `{moderator.tag}` | Moderator discriminator (always `0` after Discord's username change) |
    | `{moderator.avatar}` | Moderator avatar |
    | `{moderator.bot}` | `True` or `False` if the moderator is a bot |
    | `{moderator.color}` | Moderator's highest role color |
    | `{moderator.mention}` | Mention the moderator |
    | `{moderator.created_at}` | Moderator Discord creation date |
    | `{moderator.id}` | Moderator ID |
    | `{reason}` | Reason for the punishment |
    | `{duration}` | Duration for punishments that use it |
    | `{invite_url}` | Invite URL generated for softban rejoins |
    | `{action}` | Formatted action name (e.g. `Banned`) |

    Standard [user](#user) and [guild](#guild) variables also work. `{user}` is the punished member.
  </Tab>
  <Tab title="YouTube">
    <Info>
      Only available for `,youtube` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{youtube.channel.name}` | The YouTube channel's name |
    | `{youtube.channel.url}` | The YouTube channel's URL |
    | `{youtube.channel.id}` | The YouTube channel's ID |
    | `{youtube.channel.icon}` | The YouTube channel's icon URL |
    | `{youtube.video.url}` | The YouTube video's URL |
    | `{youtube.video.title}` | The YouTube video's title |
    | `{youtube.video.thumbnail_url}` | The YouTube video's thumbnail |
  </Tab>
  <Tab title="TikTok">
    <Info>
      Only available for `,tiktok` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{tiktok.username}` | TikTok username |
    | `{tiktok.avatar}` | Profile avatar URL |
    | `{tiktok.caption}` | Video caption |
    | `{tiktok.video.url}` | Video URL |
    | `{tiktok.video.thumbnail}` | Video thumbnail URL |
  </Tab>
  <Tab title="Instagram">
    <Info>
      Only available for `,instagram` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{instagram.username}` | Instagram username |
    | `{instagram.avatar}` | Profile avatar URL |
    | `{instagram.caption}` | Post caption |
    | `{instagram.post.url}` | Post URL |
    | `{instagram.post.thumbnail}` | Post thumbnail URL |
    | `{instagram.story.url}` | Stories URL |
  </Tab>
  <Tab title="Twitter">
    <Info>
      Only available for `,twitter` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{twitter.username}` | Twitter/X handle |
    | `{twitter.avatar}` | Profile avatar URL |
    | `{twitter.tweet.text}` | Tweet text |
    | `{twitter.tweet.url}` | Tweet URL |
    | `{twitter.tweet.media}` | First media URL when present |
  </Tab>
  <Tab title="Subreddit">
    <Info>
      Only available for `,subreddit` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{subreddit.name}` | Subreddit name |
    | `{subreddit.author}` | Post author |
    | `{subreddit.post.title}` | Post title |
    | `{subreddit.post.url}` | Post URL |
    | `{subreddit.post.thumbnail}` | Post thumbnail when available |
  </Tab>
  <Tab title="SoundCloud">
    <Info>
      Only available for `,soundcloud` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{soundcloud.artist.name}` | Artist name |
    | `{soundcloud.artist.avatar}` | Artist avatar URL |
    | `{soundcloud.track.title}` | Track title |
    | `{soundcloud.track.url}` | Track URL |
    | `{soundcloud.track.artwork}` | Artwork URL |
  </Tab>
  <Tab title="Pinterest">
    <Info>
      Only available for `,pinterest` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{pinterest.board.name}` | Board name |
    | `{pinterest.pin.title}` | Pin title |
    | `{pinterest.pin.url}` | Pin URL |
    | `{pinterest.pin.image}` | Pin image URL |
  </Tab>
  <Tab title="Twitch">
    <Info>
      Only available for `,twitch` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{twitch.streamer.name}` | Streamer name |
    | `{twitch.streamer.avatar}` | Streamer avatar URL |
    | `{twitch.stream.title}` | Stream title |
    | `{twitch.stream.url}` | Stream URL |
    | `{twitch.stream.thumbnail}` | Stream thumbnail URL |
  </Tab>
  <Tab title="Kick">
    <Info>
      Only available for `,kicker` notification messages.
    </Info>

    | Variable | Output |
    | -------- | ------ |
    | `{kick.streamer.name}` | Streamer name |
    | `{kick.streamer.avatar}` | Streamer avatar URL |
    | `{kick.stream.title}` | Stream title |
    | `{kick.stream.url}` | Stream URL |
    | `{kick.stream.thumbnail}` | Stream thumbnail URL |
  </Tab>
</Tabs>
